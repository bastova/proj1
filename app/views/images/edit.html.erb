<h1>Editing image</h1>

<% form_tag({:action => 'update', :id => @image}, :multipart=>true ) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
<% end %>

<%= link_to 'Show', @image %> |
<%= link_to 'Back', images_path %>
